import 'package:flutter/material.dart';

class CircularIcon extends StatelessWidget {

  double radius;
  // bool disabled;
  // final Function onPress;

  CircularIcon({this.radius = 20});

  @override
  Widget build(BuildContext context) {
    return GestureDetector(

    );
  }
}


